<Window x:Class="OrderDeck.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:OrderDeck.App.Views"
        Title="OrderDeck"
        Height="{StaticResource OD.Layout.AppStartHeight}"
        Width="{StaticResource OD.Layout.AppStartWidth}"
        MinWidth="{StaticResource OD.Layout.AppMinWidth}"
        MinHeight="{StaticResource OD.Layout.AppMinHeight}"
        Background="{StaticResource OD.Brush.Bg}"
        Foreground="{StaticResource OD.Brush.Text}">
    <views:MainShellView/>
</Window>
