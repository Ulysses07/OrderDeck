<Window x:Class="OrderDeck.App.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:OrderDeck.App.Controls"
        Title="Ayarlar" Width="540" Height="540"
        WindowStartupLocation="CenterOwner"
        Background="#FF1A1A1A" Foreground="White">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" Background="#FF1A1A1A" Foreground="White">
            <!-- Yazıcı tab -->
            <TabItem Header="Yazıcı">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Yazıcı:" Margin="0,8,8,8"/>
                    <ComboBox  Grid.Row="0" Grid.Column="1"
                               ItemsSource="{Binding AvailablePrinters}"
                               SelectedItem="{Binding SelectedPrinter, UpdateSourceTrigger=PropertyChanged}"
                               Margin="0,4"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Etiket genişliği (mm):" Margin="0,8,8,8"/>
                    <TextBox   Grid.Row="1" Grid.Column="1"
                               Text="{Binding LabelWidthMm, UpdateSourceTrigger=PropertyChanged}"
                               Padding="6" Margin="0,4"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Etiket yüksekliği (mm):" Margin="0,8,8,8"/>
                    <TextBox   Grid.Row="2" Grid.Column="1"
                               Text="{Binding LabelHeightMm, UpdateSourceTrigger=PropertyChanged}"
                               Padding="6" Margin="0,4"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Etiket aralığı (mm):" Margin="0,8,8,8"/>
                    <TextBox   Grid.Row="3" Grid.Column="1"
                               Text="{Binding LabelGapMm, UpdateSourceTrigger=PropertyChanged}"
                               Padding="6" Margin="0,4"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Yazı tipi:" Margin="0,8,8,8"/>
                    <ComboBox  Grid.Row="4" Grid.Column="1"
                               ItemsSource="{Binding AvailableFonts}"
                               SelectedItem="{Binding LabelFontFamily, UpdateSourceTrigger=PropertyChanged}"
                               Margin="0,4"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Kullanıcı font (pt):" Margin="0,8,8,8"/>
                    <TextBox   Grid.Row="5" Grid.Column="1"
                               Text="{Binding LabelUserFontSize, UpdateSourceTrigger=PropertyChanged}"
                               Padding="6" Margin="0,4"/>

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Mesaj font (pt):" Margin="0,8,8,8"/>
                    <TextBox   Grid.Row="6" Grid.Column="1"
                               Text="{Binding LabelMessageFontSize, UpdateSourceTrigger=PropertyChanged}"
                               Padding="6" Margin="0,4"/>

                    <Button    Grid.Row="7" Grid.Column="1"
                               Content="Test Etiketi Bas"
                               Command="{Binding TestPrintCommand}"
                               Padding="14,6" Margin="0,16,0,0"
                               HorizontalAlignment="Left"/>
                </Grid>
            </TabItem>

            <!-- OBS tab -->
            <TabItem Header="OBS">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Overlay portu:" Margin="0,8,8,8"/>
                    <TextBox   Grid.Row="0" Grid.Column="1"
                               Text="{Binding OverlayPort, UpdateSourceTrigger=PropertyChanged}"
                               Padding="6" Margin="0,4"/>

                    <TextBlock Grid.Row="1" Grid.Column="1"
                               Text="Port değişikliği için uygulamayı yeniden başlatman gerekir."
                               Foreground="#FFAAAAAA" FontStyle="Italic"
                               Margin="0,4,0,12" TextWrapping="Wrap"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Chat teması:" Margin="0,8,8,8"/>
                    <ComboBox  Grid.Row="2" Grid.Column="1"
                               ItemsSource="{Binding AvailableThemes}"
                               SelectedItem="{Binding ChatTheme, UpdateSourceTrigger=PropertyChanged}"
                               Margin="0,4"/>
                </Grid>
            </TabItem>

            <!-- Kısayollar tab -->
            <TabItem Header="Kısayollar">
                <DockPanel Margin="12">
                    <CheckBox DockPanel.Dock="Top"
                              Content="Özel kısayolları kullan"
                              IsChecked="{Binding ShortcutsTab.UseCustom}"
                              Foreground="White"
                              Margin="0,0,0,8"/>

                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Content="Kaydet"
                                Command="{Binding ShortcutsTab.SaveCommand}"
                                Padding="12,6" FontWeight="Bold"/>
                        <Button Content="Varsayılana Sıfırla"
                                Command="{Binding ShortcutsTab.ResetToDefaultsCommand}"
                                Padding="12,6" Margin="8,0,0,0"
                                Foreground="#FFFF6666"/>
                    </StackPanel>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding ShortcutsTab.Rows}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="200"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding DisplayName}"
                                                   VerticalAlignment="Center"
                                                   Foreground="White"/>
                                        <controls:ShortcutCaptureButton Grid.Column="1"
                                            Chord="{Binding Chord, Mode=TwoWay}"
                                            IsEnabled="{Binding DataContext.ShortcutsTab.UseCustom,
                                                                 RelativeSource={RelativeSource AncestorType=Window}}"
                                            Padding="8,4"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </TabItem>

            <!-- Form Linki tab -->
            <TabItem Header="Form Linki" DataContext="{Binding IntakeForm}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16">
                        <TextBlock Text="Müşteri Bilgi Formu" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"/>
                        <TextBlock Text="Müşterilerin form doldurup WhatsApp'tan size mesaj göndermesi için kalıcı bir link."
                                   Foreground="Gray" TextWrapping="Wrap" Margin="0,0,0,16"/>

                        <Label Content="Slug (URL'in son parçası)"/>
                        <TextBox Text="{Binding Slug, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Sadece küçük harf, rakam, tire. 3-32 karakter."/>
                        <TextBlock Text="Örnek: 'burakstreamer' → https://license.orderdeckapp.com/r/burakstreamer"
                                   Foreground="Gray" FontSize="11" Margin="0,2,0,0"/>

                        <Label Content="WhatsApp telefon (E.164 formatı)" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding WhatsAppPhone, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Örnek: +905551234567 (başında + ve ülke kodu olmalı)"
                                   Foreground="Gray" FontSize="11" Margin="0,2,0,0"/>

                        <Label Content="Form başlığı (opsiyonel)" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding CustomTitle, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Form sayfasının üstünde görünür. Boş bırakılırsa 'Bilgi Gönder'."/>

                        <CheckBox Content="Form aktif (kapatırsan link 410 Gone döner)"
                                  IsChecked="{Binding IsActive}" Margin="0,12,0,0"/>

                        <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                            <Button Content="Kaydet" Command="{Binding SaveCommand}" Padding="20,8" MinWidth="100"/>
                            <Button Content="Linki Kopyala" Command="{Binding CopyLinkCommand}"
                                    Padding="20,8" MinWidth="120" Margin="8,0,0,0"/>
                        </StackPanel>

                        <Border BorderBrush="LightGray" BorderThickness="1" Padding="8" Margin="0,16,0,0"
                                Visibility="{Binding HasFormUrl, Converter={StaticResource BoolToVis}}">
                            <StackPanel>
                                <TextBlock Text="Linkin:" Foreground="Gray" FontSize="11"/>
                                <TextBlock Text="{Binding FormUrl}" FontFamily="Consolas"
                                           TextWrapping="Wrap" Foreground="DodgerBlue"/>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="{Binding StatusMessage}" Foreground="{Binding StatusBrush}"
                                   Margin="0,12,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Ödeme tab -->
            <TabItem Header="Ödeme">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <GroupBox Header="WhatsApp Ödeme İsteme" Margin="12,12,12,12" Padding="8"
                              Foreground="White">
                        <StackPanel>
                            <Label Content="Mesaj Şablonu" Margin="0,0,0,2" Foreground="White"/>
                            <TextBox Text="{Binding PaymentTemplate, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True" TextWrapping="Wrap"
                                     MinHeight="120" MaxHeight="200"
                                     VerticalScrollBarVisibility="Auto"
                                     FontFamily="Consolas" FontSize="12"/>
                            <TextBlock FontSize="11" Foreground="#FFAAAAAA" Margin="0,2,0,8"
                                       TextWrapping="Wrap">
                                Placeholder: {ad} {tutar} {tarih} {iban} {hesap_sahibi} {papara}
                            </TextBlock>

                            <Label Content="IBAN" Margin="0,4,0,2" Foreground="White"/>
                            <TextBox Text="{Binding Iban, UpdateSourceTrigger=PropertyChanged}"/>

                            <Label Content="Hesap Sahibi" Margin="0,4,0,2" Foreground="White"/>
                            <TextBox Text="{Binding AccountHolder, UpdateSourceTrigger=PropertyChanged}"/>

                            <Label Content="Papara/Ziraat Numarası (opsiyonel)" Margin="0,4,0,2" Foreground="White"/>
                            <TextBox Text="{Binding Papara, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </GroupBox>
                </ScrollViewer>
            </TabItem>

            <!-- YouTube tab — Phase 5c (read) + Phase 5d (moderation) -->
            <TabItem Header="YouTube">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12,12,12,12">
                        <GroupBox Header="YouTube Live Chat" Padding="8" Foreground="White">
                            <StackPanel>
                                <Label Content="Kanal Handle (örn. @kullaniciadi)" Margin="0,0,0,2" Foreground="White"/>
                                <TextBox Text="{Binding YouTubeChannelHandle, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock FontSize="11" Foreground="#FFAAAAAA" Margin="0,4,0,0"
                                           TextWrapping="Wrap">
                                    Kanal handle'ını yaz (@ ile veya @'sız), ya da tam YouTube Live URL'si yapıştır.
                                    OrderDeck yayına başladığında otomatik bağlanır; tarayıcı açık tutmaya gerek yok.
                                    Boş bırakırsan YouTube chat takibi devre dışı kalır.
                                </TextBlock>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="YouTube Moderasyon (mesaj sil / kullanıcı banla)"
                                  Padding="8" Foreground="White" Margin="0,12,0,0">
                            <StackPanel>
                                <TextBlock Text="{Binding YouTubeConnectionStatus}"
                                           FontWeight="Bold" Foreground="White"
                                           Margin="0,0,0,8" TextWrapping="Wrap"/>

                                <StackPanel Orientation="Horizontal">
                                    <Button Content="YouTube'a Bağlan"
                                            Command="{Binding ConnectYouTubeCommand}"
                                            IsEnabled="{Binding IsYouTubeIdle}"
                                            Padding="14,7" Margin="0,0,8,0"/>
                                    <Button Content="Bağlantıyı Kaldır"
                                            Command="{Binding DisconnectYouTubeCommand}"
                                            IsEnabled="{Binding IsYouTubeConnected}"
                                            Padding="14,7"/>
                                </StackPanel>

                                <TextBlock FontSize="11" Foreground="#FFAAAAAA" Margin="0,12,0,0"
                                           TextWrapping="Wrap">
                                    Bağlandıktan sonra yayın anında chat'te bir mesaja sağ tıklayıp
                                    "YT'de mesajı sil" veya "YT'de kullanıcıyı banla" diyebilirsin —
                                    işlem doğrudan YouTube'a gönderilir. İzin scope'ları: youtube,
                                    youtube.force-ssl. Refresh token bilgisayarında DPAPI ile şifreli
                                    saklanır, OrderDeck sunucularına gönderilmez.
                                </TextBlock>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Spam tab — Phase 5f -->
            <TabItem Header="Spam">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <CheckBox Content="Spam filtresi açık"
                                  IsChecked="{Binding SpamFilterEnabled}"
                                  FontWeight="Bold" Margin="0,4,0,12"/>

                        <TextBlock Text="Aşağıdaki kuralları teker teker aç/kapat. Sadece açık kurallar uygulanır; testte kuralı kapatıp tekrar denemekten çekinme."
                                   TextWrapping="Wrap" FontSize="11"
                                   Foreground="#FF9E9E9E" Margin="0,0,0,12"/>

                        <CheckBox Content="Çift mesajları engelle (30 sn içinde aynı yazı)"
                                  IsChecked="{Binding SpamDropDuplicates}"
                                  Margin="0,4"/>

                        <CheckBox Content="Link içeren mesajları engelle"
                                  IsChecked="{Binding SpamDropLinks}"
                                  Margin="0,4"/>

                        <CheckBox Content="Tamamen büyük harfli (BAĞIRMA) mesajları engelle"
                                  IsChecked="{Binding SpamDropAllCaps}"
                                  Margin="0,4"/>

                        <CheckBox Content="Çok kısa mesajları engelle"
                                  IsChecked="{Binding SpamDropShortMessages}"
                                  Margin="0,4"/>
                        <Grid Margin="22,2,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Minimum karakter:"
                                       VerticalAlignment="Center" Margin="0,0,8,0"
                                       Foreground="#FF9E9E9E"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding SpamMinMessageLength, UpdateSourceTrigger=PropertyChanged}"
                                     IsEnabled="{Binding SpamDropShortMessages}"/>
                        </Grid>

                        <CheckBox Content="Yasaklı kelime içeren mesajları engelle"
                                  IsChecked="{Binding SpamDropProfanity}"
                                  Margin="0,8,0,4"/>
                        <Label Content="Yasaklı kelimeler (virgül ile ayır)"
                               Margin="22,0,0,2"
                               Foreground="#FF9E9E9E" FontSize="11"/>
                        <TextBox Margin="22,0,0,4"
                                 IsEnabled="{Binding SpamDropProfanity}"
                                 Text="{Binding SpamBlockedWordsText, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" TextWrapping="Wrap"
                                 MinHeight="60" MaxHeight="120"
                                 VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Validation error -->
        <TextBlock Grid.Row="1" Text="{Binding ValidationError}"
                   Foreground="#FFFF6666" Margin="0,8,0,0"
                   Visibility="{Binding ValidationError, Converter={StaticResource NullToCollapsedConverter}}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="İptal" Click="OnCancel" Padding="14,6" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Kaydet" Click="OnSave"  Padding="14,6" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
